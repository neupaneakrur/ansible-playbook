---
- name: Install Docker
  hosts: all
  become: yes

  tasks:
    - name: Update apt cache
      apt:
        update_cache: yes

    - name: Install Docker.io package
      apt:
        name: docker.io
        state: present
